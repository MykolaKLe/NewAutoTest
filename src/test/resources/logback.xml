<configuration>

    <timestamp key="bySecond" datePattern="yyyy-MM-dd_HH-mm-ss"/>

    <appender name="CONSOLE"
              class="ch.qos.logback.core.ConsoleAppender">

        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level %logger{35} - %msg%n</pattern>
        </encoder>

    </appender>

    <appender name="FILE"
              class="ch.qos.logback.core.FileAppender">

        <file>test_logs/test-${bySecond}.log</file>

        <append>true</append>

        <immediateFlush>true</immediateFlush>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{45} - %msg%n</pattern>
        </encoder>

    </appender>

    <logger name="io.github.bonigarcia"
            level="WARN"/>

    <logger name="org.apache.hc"
            level="WARN"/>

    <logger name="org.openqa.selenium"
            level="WARN"/>

    <logger name="org.testng"
            level="WARN"/>

    <logger name="ringotel"
            level="INFO"/>

    <root level="INFO">

        <appender-ref ref="CONSOLE"/>

        <appender-ref ref="FILE"/>

    </root>

</configuration>